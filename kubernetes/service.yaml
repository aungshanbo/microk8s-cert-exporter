apiVersion: v1
kind: Service
metadata:
  name: microk8s-cert-exporter
  namespace: monitoring
  labels:
    app: microk8s-cert-exporter
spec:
  selector:
    app: microk8s-cert-exporter
  ports:
    - name: metrics
      port: 9101
      targetPort: metrics